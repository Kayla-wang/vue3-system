{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  // 指定一个排除列表（include的反向操作）
  "exclude": ["node_modules", "dist", "**/*.js"],
  "include": [
    "src/**/*.ts",
    "src/**/*.vue",
    "types/*.d.ts",
    "vite.config.ts"
  ]
}
